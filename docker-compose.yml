services:
  recommendation:
    image: 210039344853.dkr.ecr.ap-northeast-2.amazonaws.com/mcm-recommendation

    ports:
      - "8000:8000"

    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_STYLE_MODEL: ${OPENAI_STYLE_MODEL:-gpt-5.6-luna}

    restart: unless-stopped
